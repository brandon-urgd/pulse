// Placeholder — Stripe checkout/portal session handler (implemented in Task 6)
export const handler = async (event) => {
  return { statusCode: 200, body: JSON.stringify({ status: 'placeholder' }) }
}
